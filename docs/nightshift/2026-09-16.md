# Nightshift 2026-09-16

**Branch**: `nightshift/2026-09-16` (from `origin/main`, one commit per Linear issue)

## Environment

| Resource | State |
| --- | --- |
| Reference sources (PostgreSQL 18.6 C tree, as vendored in pgrust) | available at `/home/user/pgrust-ref/crates/postgres-18.6-reference/src` |
| Reference binaries (PostgreSQL 18 client + server tools) | **unavailable** — byte-diff gates SKIP-flag |

### Reference sources

Sparse, shallow clone of `malisper/pgrust` at `/home/user/pgrust-ref`
(`--filter=blob:none --sparse --depth 1`), sparse-checkout limited to
`crates/postgres-18.6-reference/src/{bin/initdb,bin/psql,interfaces/libpq,test/perl,test/regress,test/modules/libpq_pipeline,include,common,port,backend/utils/misc,backend/libpq,backend/catalog,timezone,fe_utils}`.

Workers read the PostgreSQL C sources there. Per ADR-0003, **no pgrust Rust code
is ever copied into this repo**; PostgreSQL files may be copied with their
license header intact.

### Reference binaries

The PGDG apt repository is unreachable from this environment: the outbound proxy
answers `403` to `CONNECT apt.postgresql.org:443` (policy denial), so
`apt.postgresql.org.sh` added the source but `apt-get update` could not fetch its
index and `postgresql-18`/`postgresql-client-18` have no installation candidate.
`/usr/lib/postgresql/18/bin` does not exist. The only PostgreSQL client on the
box is Ubuntu's `psql` 16.13 at `/usr/bin/psql`, which is not the 18.6 reference
and must not be used as one.

Consequence: every byte-diff gate that needs a C reference binary prints
`SKIP (flagged, not silent)` and passes. No gate may be weakened or narrowed to
compensate (AGENTS.md, "Never weaken a gate to get green").

## Checks

- `cargo test --all-features` — exit 0, 46 tests passed.

## Issues

- **NAT-374** testkit byte-diff gate runner: `Gate`/`GateReport`, three cited
  normalizers, in-process `diff -U3`, rinitdb `--help`/`--version` gated
  (gate SKIP-flagged here, no PostgreSQL 18 on the box) — a96038d.
- **NAT-374** review pass: bounded the diff renderer's trace memory (543 MB →
  passes under a 48 MB cap), made the SKIP line survive libtest's output
  capture, typed the spawn error and the per-stream verdict, anchored the PID
  normalizer on a word boundary — 75df5af.
- **NAT-373** testkit TAP helpers completed: `command_ok`/`command_fails`/
  `command_like`/`command_fails_like`, `slurp_file`, `check_mode_recursive`,
  and the stdlib-only Perl-regex subset (`testkit::pattern`) the two `*_like`
  helpers needed — the `regex` crate is not approved and substring matching
  would have weakened the stolen patterns — 4a1f1e3.
- **NAT-373** review pass over 4a1f1e3: gave `(?m)^` Perl's end-of-string
  guard (`"a\n" =~ /^$/m` is nomatch), stopped an ignored directory pruning
  its whole subtree from `check_mode_recursive`, tolerated `ENOENT` from
  `read_dir` as well as from `stat`, added the vertical tab to `\s`, and moved
  the command-line formatting onto the failure path — 125 tests.
- **NAT-378** rinitdb pre-flight validation: pure `validate()` over an
  `FsProbe`, fifteen `InitdbError` variants rendering `pg_log_error` exactly,
  the `encnames.c` server-encoding table, and twelve more `001_initdb.pl`
  cases with their exact stderr — all 13 gates SKIP-flagged — 135cd7f.
- **NAT-378** review pass over 135cd7f: gave the
  `Try "%s --help" for more information.` hint one home in `help::try_help`
  instead of two `format!` literals free to drift, corrected the case count
  (twelve, not eleven), and rewrapped the gate-scope paragraph 1eaeb94 left
  with an orphaned line — 195 tests.
- **NAT-380** rinitdb datadir layout: pure `layout()` over the `subdirs[]`
  table, `file_perm.c`'s three modes as one `DataDirPerm` on the plan, `apply`
  creating the tree at exact modes without a umask, the `--waldir` → `pg_wal`
  symlink, and three stolen `001_initdb.pl` cases plus a tree-listing gate
  against C initdb (SKIP-flagged here) — 2135740.
- **NAT-380** review pass over 2135740: stopped `pg_mkdir_p` being handed the
  empty path for a relative `--pgdata` (`initdb -D mydata` failed with
  `could not create directory ""`), extracted the ancestor walk as the pure
  `missing_ancestors`, dropped an unrecorded `sync_all` from the PG_VERSION
  write, made the tree gate an equality over the paths this stage owns instead
  of a subset check, and gave the five filesystem `pg_fatal` variants their
  renderings plus four real failing-syscall cases — 219 tests.
- **NAT-379** rinitdb config generation: the three `.sample` templates vendored
  and compiled in, `conf::{replace_token, replace_guc_value, ...}` transcribed
  from `setup_config`, `pg_config`'s build-time constants, `AuthMethods` with
  the `-A` ident↔peer mirrors, the stolen `multiple --set options with
  different case`, and a four-file byte-diff gate for `-A trust` / `-A md5` /
  `--auth-host scram-sha-256` (SKIP-flagged here) — ec00620.
- **NAT-379** review pass over ec00620: gave the stolen `multiple --set options
  with different case` the `#[cfg(unix)]` its `create_plan` helper needs (the
  test target did not compile off Unix), gave the compiled-in-templates
  divergence a pin that bites — length plus digest of all three `.sample`
  files, checked non-vacuous — dropped a blanket
  `allow(clippy::too_many_lines)` that turned out to be unnecessary, and
  stopped `render_pg_ident_conf` round-tripping a `Vec` to return its own
  argument — 270 tests — a616024.
- **NAT-388** rlibpq conninfo + URI parsing: `PQconninfoOptions[]`'s fifty rows
  in upstream order, `conninfo_parse` / `conninfo_uri_parse_options` / the
  percent-decoder / `conninfo_add_defaults` over an explicit `Env`, values kept
  as bytes so a percent-decoded non-UTF-8 token survives, a
  `libpq_uri_regress` binary, all 63 rows of `t/001_uri.pl` and a byte-diff
  gate over them (SKIP-flagged: the C helper is a test program no package
  ships). `testkit` gained `Environment` (`Utils.pm`'s env scrub as data),
  `run_in` and `Gate::with_env` — 56a8ee2.
- **NAT-384** rinitdb sync options: `rinitdb::sync` as `sync_pgdata` /
  `walkdir` / `fsync_fname` over a `SyncProbe`, `parse_sync_method` in its
  getopt arm, `--sync-only` wired into `run`, and the three stolen cases
  `sync only` / `--no-sync-data-files` / `sync method syncfs` byte-diff gated
  (SKIP-flagged here). `syncfs` is accepted where `HAVE_SYNCFS` says it should
  be but plans the fsync walk — two new divergence rows — 914c464.
- **NAT-384** review pass over 914c464: told a failed `readdir` apart from a
  failed `opendir` in `walkdir` — the first is `could not read directory`
  (`file_utils.c:337`) after the names it did read, and still fsyncs the
  directory, where the port had been printing `could not open directory` and
  syncing neither — stopped `succeeds_with` spawning the binary twice per
  stolen case, and labelled the `SyncMethod` match as the exhaustiveness
  guard it is — 344 tests — e2eb87d.
