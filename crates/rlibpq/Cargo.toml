[package]
name = "rlibpq"
description = "Pure-Rust libpq client: native crate and, later, a C-ABI libpq.so"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
publish.workspace = true
license = "MIT"

[lib]
path = "src/lib.rs"

[dependencies]

[lints]
workspace = true
